/*
    In this File we use  include function to check the value is 
    contained in define array or not.
    that include function return Boolean value (True, False)
    If value is contain in array it return True otherwise It Return False
*/

// In this example it return -1 or 1 
let numArray1 = [1,2,4,5,7]
console.log(numArray1.indexOf(2));


// In this example it return False or True
export let numArray = [1,2,4,5,7]
console.log(numArray.includes(2));

